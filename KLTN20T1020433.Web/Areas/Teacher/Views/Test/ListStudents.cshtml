@using KLTN20T1020433.Application.DTOs.TeacherDTOs
@model IEnumerable<GetStudentResponse>
@{
    Layout = null;
}
<input type="text" id="searchInput" class="form-control mb-3" placeholder="Search...">
<div class="mb-3">
    <button id="selectAllBtn" class="btn btn-primary mr-2">Select All</button>
    <button id="clearAllBtn" class="btn btn-danger">Clear All</button>
</div>

<ul class="list-group">
    @foreach (var item in Model)
    {
        <li class="list-group-item d-flex justify-content-between align-items-center">
            <div>
                <input type="checkbox" class="form-check-input me-2" id="student1Check" name="studentSelecting" value="@item.StudentId">
                <strong>Mã SV: </strong>@item.StudentId - <span>Họ và tên: @item.LastName @item.FirstName </span>
            </div>
        </li>
       
    }
</ul>
<script>
    $('#selectAllBtn').click(function () {
        $('#studentList input[type="checkbox"]').prop('checked', true);
    });

    // Clear all button click event
    $('#clearAllBtn').click(function () {
        $('#studentList input[type="checkbox"]').prop('checked', false);
    });
</script>